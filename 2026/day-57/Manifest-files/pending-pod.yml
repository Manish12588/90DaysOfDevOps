apiVersion: v1
kind: Pod

metadata:
  name: pending-pod
  labels:
    app: pending-pod

spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      requests:
        cpu: 100
        memory: 128Gi