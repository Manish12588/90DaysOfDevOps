apiVersion: v1
kind: Pod

metadata:
  name: resource-pod-guaranteed
  labels:
    app: resource-pod-guaranteed

spec:
  containers:
  - name: nginx
    image: nginx:latest
    resources:
      limits:
        cpu: 250m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 256Mi